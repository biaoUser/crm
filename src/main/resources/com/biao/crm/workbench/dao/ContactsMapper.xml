<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biao.crm.workbench.dao.ContactsMapper">
    <select id="getContactsListByName" resultType="contacts">
        select id, fullname,email,mphone from tbl_contacts where fullname like '%' #{aContactsName} '%'
    </select>
	
  	<insert id="save" parameterType="contacts">
         insert into tbl_contacts(

            id,
            owner,
            source,
            customerId,
            fullname,
            appellation,
            email,
            mphone,
            job,
            birth,
            createBy,
            createTime,
            description,
            contactSummary,
            nextContactTime,
            address
        )

        values(

            #{id},
            #{owner},
            #{source},
            #{customerId},
            #{fullname},
            #{appellation},
            #{email},
            #{mphone},
            #{job},
            #{birth},
            #{createBy},
            #{createTime},
            #{description},
            #{contactSummary},
            #{nextContactTime},
            #{address}


        )

    </insert>
</mapper>

































